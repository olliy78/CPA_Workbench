; Hello World!! fuer CP/M (M80-Assembler, 8080-Syntax)
; Gibt "Hello World!!" auf der Konsole aus und kehrt zum CCP zurueck

        org     100h

start:  lxi     d, msg      ; DE zeigt auf den Text
        mvi     c, 9        ; CP/M-Funktion 9: Print String
        call    5           ; BIOS-Sprungvektor

        ret                 ; Zurueck zum CCP

msg:    db 'Hello World!!$'

END     start
